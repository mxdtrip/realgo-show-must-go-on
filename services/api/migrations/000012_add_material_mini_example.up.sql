-- Мини-пример — шестая секция методической единицы субпаттерна
-- (что это → как распознать → механика → инвариант → skeleton → мини-пример).
ALTER TABLE pattern_learning_materials
    ADD COLUMN mini_example TEXT NOT NULL DEFAULT '';
