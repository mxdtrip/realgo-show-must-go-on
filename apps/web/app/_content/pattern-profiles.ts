// Контент страниц паттернов (семейств атласа). Структура секций фиксирована:
// «Что это» → «Как распознать» → «Ключевая механика» → «Когда не подходит» →
// заметки к подпаттернам. Материал заполняется вручную по коду семейства;
// пустая секция — страница честно показывает заглушку «материал готовится».

export type PatternProfileContent = {
  /** «Что это» — 2–3 предложения, каждый элемент = абзац. */
  what?: string[];
  /** «Как распознать» — главные признаки в условии, по одному на элемент. */
  recognize?: string[];
  /** «Ключевая механика» — что именно двигается / хранится / обновляется. */
  mechanics?: string[];
  /** «Когда не подходит» — 2–3 типичных ложных сигнала. */
  misfits?: string[];
  /** Чем подпаттерны отличаются: код подпаттерна → одна фраза. */
  subpatternNotes?: Record<string, string>;
};

export const patternProfiles: Record<string, PatternProfileContent> = {
  /* 01 · Hashing / Frequency */
  arrays_hashing: {},

  /* 02 · Two Pointers — ЧЕРНОВИК для демонстрации вёрстки, заменить присланным материалом */
  two_pointers: {
    what: [
      "Два указателя — способ обойти линейную структуру (массив, строку, список) не вложенными циклами, а двумя индексами, которые двигаются по заранее оговорённым правилам. Вместо перебора всех пар за O(n²) указатели за один проход сужают пространство поиска до O(n).",
      "Паттерн опирается на порядок: чаще всего данные отсортированы, либо само движение указателей поддерживает нужный инвариант.",
    ],
    recognize: [
      "Массив отсортирован — или его можно отсортировать без потери смысла задачи.",
      "Просят пару/тройку элементов с условием на сумму, разность или расстояние между ними.",
      "Нужно изменить структуру «на месте», без дополнительной памяти: развернуть, удалить дубликаты, переставить.",
      "Сравнение идёт с двух концов: палиндром, наибольший «контейнер», встречное движение.",
    ],
    mechanics: [
      "Хранится: два индекса (left/right либо slow/fast) и лучший найденный ответ.",
      "Двигается: на каждом шаге ровно один указатель — какой именно, решает сравнение текущих значений с целью.",
      "Обновляется: инвариант «всё за пределами [left, right] уже проверено и отброшено» — благодаря ему каждый элемент рассматривается не более двух раз.",
    ],
    misfits: [
      "Данные не отсортированы и сортировать нельзя (важны исходные индексы) — чаще это complement lookup по хеш-таблице.",
      "Нужно «окно» с агрегатом (сумма, счётчик символов) переменной длины — это sliding window, хотя указателей тоже два.",
      "Условие не монотонно: сдвиг указателя не даёт однозначного «стало лучше или хуже», сужение пространства не обосновано.",
    ],
    subpatternNotes: {
      opposite_direction_pointers:
        "Указатели стартуют с концов и идут навстречу — сумма пары, палиндром, контейнер с водой.",
      same_direction_read_write:
        "Оба идут слева направо: read сканирует, write собирает результат — удаление и уплотнение на месте.",
      fast_slow_pointers:
        "Разная скорость движения — поиск цикла, середины списка, k-го элемента с конца.",
    },
  },

  /* 03 · Sliding Window */
  sliding_window: {},

  /* 04 · Prefix & Cumulative State */
  prefix_cumulative: {},

  /* 05 · Binary Search */
  binary_search: {},

  /* 06 · Intervals & Sweep */
  intervals: {},

  /* 07 · Stack & Monotonic Structures */
  stack: {},

  /* 08 · Heap / Priority Queue */
  heap: {},

  /* 09 · Linked List Techniques */
  linked_list: {},

  /* 10 · Tree Traversal & Structure */
  trees: {},

  /* 11 · Graph Traversal */
  graphs: {},

  /* 12 · Topological Ordering */
  topological_ordering: {},

  /* 13 · Union Find / Connectivity */
  union_find: {},

  /* 14 · Shortest Paths */
  shortest_paths: {},

  /* 15 · Backtracking & Search */
  backtracking: {},

  /* 16 · Greedy */
  greedy: {},

  /* 17 · Dynamic Programming */
  dynamic_programming: {},

  /* 18 · Trie & String Search */
  tries: {},

  /* 19 · Bit Manipulation */
  bit_manipulation: {},

  /* 20 · Matrix & Grid */
  matrix_grid: {},

  /* 21 · Cyclic Placement / Index Mapping */
  cyclic_placement: {},

  /* 22 · Divide & Conquer / Selection */
  divide_conquer: {},
};
