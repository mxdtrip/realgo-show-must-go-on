# Docker Compose local defaults.
# Copy to .env and replace AUTH_JWT_SECRET before starting the stack.

DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=freeburger
DB_PORT=5432

REDIS_PORT=6379
REDIS_PASSWORD=

API_PORT=8080

AUTH_JWT_SECRET=replace-with-at-least-32-random-characters
AUTH_ACCESS_TTL=15m
AUTH_REFRESH_TTL=720h

# Optional comma-separated CIDRs for reverse proxies whose X-Forwarded-For
# headers should be trusted by the API rate limiter.
TRUSTED_PROXY_CIDRS=

# Set this to the published Chrome extension origin, for example:
# chrome-extension://abcdefghijklmnopabcdefghijklmnop
REALGO_EXTENSION_ORIGIN=

# Base URL baked into the web app for API calls. Leave EMPTY in the tunnel setup:
# the frontend and API share the realgo.dev origin (Caddy routes /api/* to the
# api container), so relative URLs work and no CORS is needed.
NEXT_PUBLIC_API_BASE_URL=

# Cloudflare Tunnel token (Zero Trust -> Networks -> Tunnels -> your tunnel).
# Publishes the stack on realgo.dev with no open inbound ports. Required to start
# the cloudflared service.
CLOUDFLARE_TUNNEL_TOKEN=

# VLESS subscription URL for the vpngw gateway. The provider blocks Cloudflare by
# SNI DPI, so cloudflared's edge traffic is tunnelled through this VLESS/Reality
# subscription (port 443, looks like normal HTTPS). Required to start vpngw.
VPN_SUB_URL=
